import React from 'react'
const initialState = {
    data:[]
}

const BlogReducer = (state=initialState, action)=>{
    switch(action.type){
        case "data":
        return state
    }

}

export default BlogReducer
